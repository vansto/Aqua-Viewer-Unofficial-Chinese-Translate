version: 1.0.{build}
environment:
  nodejs_version: "12"
install:
 - ps: Install-Product node $env:nodejs_version
 - cmd: npm install
build_script:
- cmd: >-
    npm install -g @angular/cli

    npm audit fix --force

    npm run build
artifacts:
- path: dist\aqua-viewer
